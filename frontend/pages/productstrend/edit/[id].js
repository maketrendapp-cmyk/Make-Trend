// pages/productstrend/edit/[id].js
// This page re-uses the launch page with edit mode enabled.
// The `id` from the URL will be available in `launch.js` via `router.query.id`.
export { default } from '../launch';